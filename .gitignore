# Third-party asset pack source (licensed; not redistributed)
art/
art.zip

# Dev artifacts (video frame extracts, screenshots, etc.)
notes/video_frames/
notes/screenshots/
match_board_shot.png

# Claude Code local state (plans, local settings, locks)
.claude/

# Local-only secrets — NEVER commit. Nakama console login + the Godot client's
# server-key config (read at runtime by the NakamaClient autoload).
deploy/nakama/*.local.txt
src/nakama_local.cfg

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Android build artifacts
*.apk
*.apk.idsig
*.aab
*.pck
mbtd_probe*
.nettest_logs/

# Dedicated-server build output (exported per-machine; rebuilt via deploy/README.md)
deploy/build/
collection_shot.png
season_shot.png
nudge_shot.png
