# State — Wend
Last updated: 2026-06-08

> **Session wrap 2026-06-08 (CC — juice: meta-menu complete + cross-scene transition):** Finished the meta-menu surface. Home attitude + entrance and both select-card cascades were committed (`6fe4789`, approved live by Tobe). This adds the third piece — the **cross-scene slide transition** (`scene_manager.gd`), the "connective tissue." Since SceneManager swaps scenes with a hard `change_scene_to_file`, the transition **snapshots the outgoing screen** (`get_viewport().get_texture().get_image()`) into a TextureRect **cover** on a top CanvasLayer (layer 128), swaps the scene underneath, then **slides the cover off** via Motion (forward wipes left, back/home wipes right) — revealing the new screen as it plays its own entrance. Scoped to the **5 menu navigations** (home/pve_select/campaign_select/leaderboards/lobby); entering a MATCH stays a hard cut (gameplay setup shouldn't ride a menu wipe). Guards: first-nav (boot→home hard-cuts, nothing to snapshot), empty-snapshot (headless → hard cut), and a 1s safety timer that frees the input-blocking cover so a failed tween can't soft-lock navigation. Parse clean; **transition feel is a playtest item** (launched for Tobe to verify). **Meta-menu now ✅ complete.** Remaining juice surfaces: staged climbs (Ranked Surface 2 LP fill + ghost ladder), pause/settings arrivals. NOTE: the cross-scene transition is **uncommitted** (post-`6fe4789`).

> **Session wrap 2026-06-08 (CC — juice: meta-menu home attitude + entrance):** Started the meta-menu surface (`home_screen.gd`). The home now wears the locked break-the-grid attitude from `meta_menu_mock.html` — **Trials rides high, Ranked rides low, both tilted −2.5°** off-axis — and **arrives staggered** on load (season + WEND title ease in → heroes drop into their off-axis offsets, earning L → Campaign rises → corners fade in last). To offset + animate the heroes inside an HBox without the container stomping their position, each hero sits in a plain Control **slot** (HBox manages the slot; the hero is free inside). **This is a visible composition change** (the heroes were a flat centered row before) — design-locked in the mock, but worth Tobe's eyes; captured mid + settled. Parse clean. **Remaining meta-menu:** the **home→select cross-scene transition** (SceneManager uses hard `change_scene_to_file` → needs a snapshot/overlay layer; architectural) and the **select-screen card cascade** (contained, per-screen). NOTE: this home work is **post-commit `319ecdd`** (uncommitted).

> **Session wrap 2026-06-08 (CC — juice: in-match HUD rail + tower-deepen pop):** Second juice surface, felt every match. `rail.gd` — the three rail boxes (Status / Score / Buttons) now **cascade in** on match start, staggered top→bottom (0.20/0.29/0.38s), armed-before-reveal. **Key learning (documented in JUICE.md):** the boxes live in a VBoxContainer that re-sorts when the status text refreshes (build timer ticks) and **stomps any animated `position`** — confirmed via a numeric capture probe (boxes snapped to their slot x mid-tween). So the arrival rides **scale (0.9→1 arrive overshoot) + fade**, render transforms the container can't touch (the same reason the victory stars/buttons used scale, not position). `tower.gd` — `upgrade()` now fires an **emphasis-pop** on the tower sprite at the instant it deepens (the colour ramp stays the locked identity, unchanged/instant; the pop is the only juice), guarded to an in-tree sprite so the headless re-sim replay stays clean + deterministic. Two `motion.gd` fixes along the way: **`pop` now scales about the node's CURRENT scale** (so the 0.12-scaled tower sprite pops correctly instead of blowing up to 1.0), and **`slide_in` had a `set_parallel`-before-move bug** that made staggered slides ignore their `delay` — fixed (move sequential after the interval, alpha parallel to the move). **Verified:** mid-cascade capture (box3 arriving while box1 settled) + full regression — sim_harness ✅ (re-sim==live dmg=53225, determinism intact), ghost_ladder ✅, campaign_verify ✅, motion_test ✅, leaderboard_test ✅. **Feel is a playtest item.** Remaining juice: meta-menu transitions, staged climbs (Surface 2 + ghost ladder), pause/settings/select arrivals.

> **Session wrap 2026-06-08 (CC — juice: victory choreography):** First juice set-piece implemented against the locked `design/JUICE.md` + `victory_screen_mock.html` (the validated reference). `match_end_panel.gd` campaign victory now stages instead of appearing instantly: scrim **dims** (M) → gold **hero drops in** tilted (earns the L duration) → **3 star tiles cascade low→high** (130ms set-piece stagger), each earned tile **pops on land** → **DAMAGE fades + ticks 0→value + pops** → leave-only **buttons settle in** staggered. All routed through `Motion` (no re-invented curves), **armed-before-reveal** (alpha 0 before show, no end-frame flash), reduced-motion compresses via `Motion.dur()`. Implementation note: stars/buttons animate on **scale** (not position) so the score-tick's text-driven re-layout of the column can't stomp them mid-animation; only the hero (settled ~0.6s, well before the 1.08s tick) uses a positional drop. New members `_score_block`/`_victory_damage`; new methods `_play_victory_choreo`/`_tick_score`/`_pop_after`; `_star_tile` marks earned via meta. **Verified** by mid + settled windowed captures (`tools/victory_shot.{gd,tscn}`, NEW): board dims, hero tilts, stars land gold, DAMAGE ticks to 9,000, buttons settle — composition + staging correct. **Tween feel is a playtest item** (motion isn't headless-testable). Parse-check clean. **Flagged (pre-existing, NOT this pass):** the in-match rail HUD stays visible above the scrim at match-end (a layering call from the rebuild; the mock shows no rail) — separate from motion, worth a deliberate decision later. **Remaining juice surfaces** (HUD rail stagger-in + tower-deepen pop, meta-menu transitions, staged climbs, pause/settings) inherit the helper — not yet built.

> **Session wrap 2026-06-08 (CC — polish-list closeout + Wend rename):** Cleared the rest of the open polish punch-list before resuming juice (Tobe's call: finish polish + new-design first). **#1 white corners — NOT REPRODUCIBLE:** captured the home screen at 1080p + 8× crops of all four corners (`tools/home_shot.{gd,tscn}`, NEW) — every screen corner is clean dark grass, zero white pixels; with the in-game board corners also clean, the render has no white anywhere. Attributed to **Windows 11 OS window-corner rounding** (windowed-only compositor effect, absent in fullscreen) — no code change, documented. **#2 text-too-thin — ACTUALLY FIXED (my first "done" was wrong):** a weight-comparison capture (`tools/font_sample.{gd,tscn}`) exposed the real bug — `fredoka_bold.tres` used `variation_opentype = {"wght": 600}` with a **STRING key**, which Godot silently ignores, so the variable font stayed at its Light (300) default everywhere. Fix: integer OpenType tag `{ 2003265652: 700.0 }` (`0x77676874`='wght'), bumped to **700/Bold**; verified thick in the home capture. Memory `reference_godot_variable_font_weight` corrected (it had wrongly said the string key works). **#3 em-dash sweep — DONE:** replaced em-dash separators in user-facing strings with the house middot "·" or rephrased (`pve_select`, `match_end_panel`, `lobby` ×4, `leaderboard_browse` ×2) + fixed the `VISUAL_SYSTEM.md` "em-dash if unplayed" drift → "No score yet"; left bare-value `"—"` placeholders and dev `push_warning`s (not player-facing). **New-design: the locked "Wend" rename** — home title "MAZE BATTLE TD" → **"WEND"** + `project.godot config/name` → "Wend" (Tobe chose to keep the "Build the maze. Milk the horde." tagline as the subtitle). Verified by home capture. **Polish list is now fully closed** (every item resolved or playtest-only). Regression: `leaderboard_test` ✅, `campaign_verify` ✅, full parse-check clean. **Juice is now unblocked per Tobe's gate.**

> **Session wrap 2026-06-08 (CC — campaign tutorial follow-ups):** Closed the remaining code items from the 2026-06-08 copy-pass follow-up list (with `campaign_verify` now **green across all 5 missions** — the PASS-1 maze/economy decouple from `08d2d42` resolved the false M2/M3/M4 failures). Two behavioral changes: **(1) ghost outline clears on deviation** (`build_guide.gd` — `refresh()` now retires the WHOLE outline the moment a tower lands off the suggested set; tracks the full suggested set in `_suggested`; on-suggestion builds still clear just that cell and the outline persists — matches M3's "your choice whether to follow it" copy). **(2) tutorial toasts are dismissable-only** (`tutorial_callout.gd` — removed the 6s auto-dismiss timer; a toast now waits for a player tap, with a faint "Tap to dismiss" hint added to the card; blocking modal unchanged). **Already-done items confirmed:** M3 outline rides its single load beat (the load beat carries `ghost_cells`, persists into build); M1 win flow leave-only (victory rebuild); `grid_size` default `25×16` (`map_resource.gd:16`); threshold fields kept (1/2/3-star labels). **Verified:** added `campaign_verify` PASS 3 — a stub-controller unit test of the guide (`GUIDE ✅ on-suggestion build keeps outline; deviation clears it`); full project parse-check clean; all 5 missions still ✅. **Remaining campaign items are playtest-only:** anchor resolve check in the new rail HUD (#6), M1's blocking opener pause→resume (not headless-testable).

> **Session wrap 2026-06-08 (CC — victory/result screen rebuild):** Second "screens-final-before-juice" rebuild. Rebuilt the **campaign result screen** (`match_end_panel.gd` `_show_medal` → new `_show_campaign_victory`) to `notes/mockups/victory_screen_mock.html`: a **scrim-dimmed board** behind, an **angled gold VICTORY hero** (-3.5°, "MISSION n · NAME") overlapping the board, three **square star tiles with a full clean outline** (gold earned / card empty — **resolves polish #7**, the corner-only-outline look is gone), the DAMAGE score, and **leave-only Next map / Trials / Ranked** buttons (**resolves the campaign M1-win-flow follow-up #4** — mission-end is leave-only → next map / Trials / Ranked). Scope kept tight: PVE-Trials/PVP/ranked/eliminated keep their existing card layout (out of the mock's scope; data-dense, Steam-gated), and a shared scrim now dims the board behind ALL result modes. **Static composition only — the staged reveal (cascade/pop/tick) is the later juice pass** and slots onto these elements. Verified by windowed capture (campaign mission 3, 1-star) + headless build regression (ghost_ladder / leaderboard green; match_end builds in PVE + campaign). `star_rating.gd` unchanged (still used for the small star indicators in campaign-select / pve-select / pause).

> **Session wrap 2026-06-08 (CC — in-match HUD rail rebuild):** Implemented the locked rail layout (`design/INMATCH_HUD.md`) — the first of the "screens-final-before-juice" rebuilds (Tobe redirected: don't juice screens that are about to be rebuilt; see memory `feedback_screens_final_before_juice`). Replaced the three-zone floating layout (top-bar HUD pills + bottom action strip + permanent right inspector dock) with **ONE reserved right rail** (`src/scripts/rail.gd`, NEW) holding three boxes — Status / Score-or-Standing / Buttons — plus a **maximized 25×16 board** centered in the remainder, and **tower info that docks in the rail's lower gap** beneath the buttons (with an automatic over-board **overlay fallback** on windows too short to fit it — Tobe's pick after I measured ~175px clearance at 1080p). **Verified by windowed capture** (`tools/ui_shot.gd`): rail renders correct (board 25×16 at ~65px tiles, Speed greyed in build, campaign star thresholds in the SCORE box), and the tower panel **docks in-rail with zero board occlusion** on tower-select. Folded in polish-punchlist items this surface owns: **#6** (the layout), **#5** (tower hide button gone), **#4** (Speed disabled during build + a speed-index→engine desync fix), **#8** (placement ghost stops when out of supply). **Board locked 25×16 everywhere** (`grid.gd ROWS 14→16`, `map_resource` default; generated maps follow). New: `rail.gd`, `build_confirm.gd` (touch-only). Rewritten: `tower_drawer.gd` (contextual, dock-or-overlay), `ui_layout.gd` (rail model), `game_view.gd` (+`board_screen_rect`, spectate frame brackets board), `map_loader.gd`. Add-only: `ghost_ladder.rungs_above`. **Deleted: `hud.gd`, `action_strip.gd`.** Headless regression green (motion / ghost_ladder / leaderboard / sim_harness). **⚠️ NOTE (pre-existing, NOT from this work): `campaign_verify` reports M2/M3/M4 ghost-cell failures — but per Tobe the VERIFIER is broken, not the maps (the cumulative-placement assertion is wrong; the hand-authored mazes are fine). Confirmed identical failure at HEAD/ROWS=14 (so not the rebuild). Being diagnosed separately — do NOT re-author the maps.** Remaining HUD-adjacent: tower dock/overlay *feel* + Esc/select dismiss = playtest; PVP Standing box needs a PVP capture; board white-corner (#1) didn't reproduce in capture.

> **Session wrap 2026-06-08 (CC — juice implementation: the shared motion helper):** Built the keystone the locked `design/JUICE.md` mandated first — **`src/scripts/motion.gd`**, the single source of timing + easing so no surface re-invents motion ("one authored hand"). Preload-static like `UiStyle`. Provides the timing tokens (XS90/S160/M260/L440/SCREEN320), the three verbs (`arrive`/`settle`/`leave` — usable on a whole Tween *or* one tweener), a **faithful distance-independent `arrive_ease`** (easeOutBack, not Godot's distance-coupled TRANS_BACK), `pop`/`fade_in`/`fade_out`/`slide_in`/`arrive_property`, `cascade`/`stagger_delay` (capped at 0.42s), and a `reduced`-motion flag + `dur()` scaler wired through everything (Settings toggle still to wire). **Pure math verified headless** — `src/tools/motion_test.{gd,tscn}` (arrive curve · reduced-motion · stagger cap · tokens, all ✅). **First adoptions:** the two surfaces that hand-rolled tweens now route through `Motion` — the **wave-clear toast** (`round_toast.gd`: drops from the top on the arrive curve → holds → leaves faster; a named JUICE beat) and the **in-match PVP leaderboard drawer** (`leaderboard_panel.gd`: arrive-in / leave-out). Parse + match-build re-verified via `ghost_ladder_test` ✅; **tween feel is a playtest item** (not headless-testable). **Overshoot dial flagged:** the mock's literal bezier overshoots only ~3.4% but the doc prose + CC Godot-map say ~10–12% — went with the stated 11% (`ARRIVE_OVERSHOOT_S=1.8`); shape is locked, magnitude is a playtest dial (drop toward 1.0 for the gentler mock feel). Remaining surfaces (HUD rail/pop, victory, meta-menu, staged climbs, pause/settings) inherit the helper — not yet built.

> **Session wrap 2026-06-08 (design — juice / game-feel pass):** Tobe reframed juice himself (the parked direction): **juice is NOT audio** (TD players mute — no shooting chatter, no EDM); it's transitions, animations, impact events, staged set-pieces, and Atlus/Persona-style UI that overlaps the play surface — the **principle, not the skin** (a Fredoka-warm comic-poster register, NOT red/black). Locked the foundation into **`design/JUICE.md`** (lens + motion language + break-the-grid grammar) and validated it across **six reference mocks** in `notes/mockups/`: motion reference, victory, in-match HUD, meta menu, staged climbs, round-end overlay. Locks: motion = timing scale (XS90/S160/M260/L440) + three easing verbs (arrive w/ ~10–12% overshoot · settle · leave) + emphasis-pop + 60ms stagger + the **"board stays calm, juice in the frame"** guardrail + an **arm-before-reveal** sequencing rule; break-the-grid = one tidy box, text on the box's angle, depth from bevel+shadow (**no stacked-duplicate shape** — rejected), overlap is box-over-surface, precision targets stay square. Tower color-deepen on upgrade = **existing ramp, unchanged**; the only juice is the pop on the step. Remaining surfaces (pause / settings / campaign select / supply-out cursor) **inherit the grammar** — no separate mocks. **Flagged drift:** `VISUAL_SYSTEM.md` PVE-card line still says "em-dash if unplayed" (a user-facing string) → logged to the punchlist. No CC blockers added; juice is ready for CC to implement against `design/JUICE.md` + the mocks.

> **Session wrap 2026-06-08 (design — campaign map editor + tutorial copy):** Built a hand-authoring **map editor** (`notes/tools/map_editor.html`) at the locked **25×16** — paints entry / exit / ordered checkpoints / obstacles / tower-ghosts + bonus zones (radius auto-derived from the locked `radius_for_magnitude` formula), validates a legal entry→checkpoints→exit path (4-dir BFS), imports an existing `.tres` **losslessly** (tutorial beats preserved verbatim, build-phase ghost geometry rewritten from the painted cells), and **saves real `mission_NN.tres`** for batch-push. Closes the editor half of polish item #9. **Heads-up:** the first author pass (pushed `b2b8e58`) was done WITHOUT importing, so it wiped `tutorial_beats` on all five and defaulted names/descriptions — now **fixed**. All 5 missions rebuilt: reviewed tutorial copy (walked beat-by-beat with Tobe — undead framing, no em-dashes anywhere, medals→leaderboard/stars language, several beats cut), correct names (**First Contact · The Long Way · Switchbacks · Hot Spots · The Gauntlet**), real descriptions, layouts + zones + ghost outlines preserved. **Six CC follow-ups under Next step.** Editor's repo home: `notes/tools/map_editor.html`.

> **Session wrap 2026-06-07 (design — in-match HUD):** full in-match layout designed + signed off, locked in `design/INMATCH_HUD.md`. Reserved **right rail** (status / score-or-standing / buttons), **maximized board** — cell count is now **25×16** (derived once at the 1080p reference using the current on-screen cell size + a 280px rail; locked **universal**, other resolutions scale + center the same grid), and **tower info as a contextual overlay** over the board (not a reserved dock), hide-button removed, Sell label bare. Reference mock: `notes/mockups/inmatch_assembly.html`. **This unblocks the campaign rebuild + the map editor** (board size was the dependency — editor authors against 25×16). Also recorded a 9-item polish/bug punch-list → `notes/polish_punchlist.md` (CC tasks). **Juice pass parked** — Tobe is reframing the throughput / Persona-Atlus direction himself before any spec is built; do not treat Claude's earlier framing as locked.

> **Session wrap 2026-06-07:** two pieces shipped + deployed live — **Ranked LP/MMR engine + Surface 2** (the deferred ranked-scoring piece; MMR plumbed through Nakama, verified `GO avg_mmr`) and **server-owned Trials seeds** (resim §10 seed tail; `trials_seeds` RPC, verified live). The anti-cheat/scoring spine is now complete for Trials/campaign/ranked. **Everything left is Steam-gated** (distribution → human 2-client E2E) or wants a human (interactive solo playtest). USER → design work until Steam approves. See the two ✅ COMPLETE records under "Next step".

> **Read order:** `claude-rules.md` → `RULES.md` → this file → `notes/open_items.md` (full backlog) → only the specific file the task needs.
> **History:** older session logs were moved to `STATE_ARCHIVE.md` — reference only, don't load unless you're digging into a past decision.

---

## ⚠️ Recent reversals — do NOT act on stale instructions
- **Platform: PC/Mac-first. Mobile NEVER** (console if it succeeds; mobile only on viral success). Mobile would be a *fork*, not a port.
- **Board is 25×14** (locked + implemented). The old 20×11 mobile shrink is dead.
- **In-match UI = the 2026-06-05/06 v3 bounded layout** (recessed surround + bright bordered board, right inspector dock, redesigned victory panel).
- **Pricing: $10–15 (PC band).** Saves = Steam Cloud.
- **No disposable intermediates (NEW 2026-06-06).** We build toward the end state, not throwaway rungs. itch.io beta is DEAD → **closed Steam beta** is the target. Re-sim/anti-cheat and real queue-based multiplayer (Option B) are **pulled forward**, not deferred. See `notes/open_items.md` "session 3".
- **Backend box moved (2026-06-08).** New box = **`5.78.110.182`** (CPX31, 4 vCPU / 8 GB / 160 GB, Hetzner **`hil`/us-west**, Ubuntu). Old CPX11 at `ash` is **deleted**. Any reference to `178.156.171.215` or "resize the box" is stale → see `deploy/nakama/README.md` §0.

---

## Name
**"Wend"** (locked). Subtitle carries genre. **Modes: Trials (PVE), Ranked (PVP).**
**Trials scale names: Thread · Weave · Tangle · Snarl · Knot** (1→5, locked 2026-06-06).

---

## Steam (closed beta) — account status [2026-06-07]
First step toward the closed Steam beta is taken: **Steamworks partner account registered** against the existing personal Steam account (long-standing, Steam Guard on).
- **Entity type:** registered as dev/publisher — individual-vs-company choice NOT confirmed in chat. **CONFIRM** which was selected (matters for tax/bank + later restructure).
- **$100 Steam Direct fee: PAID.** → **30-day release clock is running.** Earliest possible main-game release ≈ **2026-07-07**.
- **Tax (W-9, MI) + bank (checking / USD / US): submitted.** → **Identity Verification Pending, 2–7 business days** (third-party service Lilaham/TaxIdentity). Cannot change tax/bank info or finish account creation until it clears.

**Blocked until verification clears (next actionable Steam step):**
1. Create the **Wend App ID** (the main game app).
2. Create the **Playtest app** off it — Associated Packages & DLC page → set Library capsule image + visible name ("Wend - Playtest") → walk the release-process checklist.

**Open decision to make next session (drives the Playtest config):**
- **Confidential / friends-only:** Playtest App ID + request Standard Release keys + set Playtest Playable + Store Visibility Hidden. No public page, no 2-week wait, nothing leaks. Hand keys directly to testers. *(Likely the right call for a few-friends beta.)*
- **Public Coming Soon page + Playtest signup button:** more setup, reveals Wend publicly, and the store page must be live ≥2 weeks before main-game release — but starts banking wishlists immediately.

(Steam *closed-beta mechanics* design — what the beta build actually contains/tests — remains a separate open design thread; see below.)

---

## Current focus
**In-match HUD layout designed + signed off (2026-06-07)** — `design/INMATCH_HUD.md`. Board count locked **25×16** (universal, scale+center off 1080p). This was the dependency blocking the campaign rebuild + map editor — both now unblocked. Next design candidates: campaign rebuild/editor, juice (parked pending Tobe's reframe), Steam beta mechanics, GTM, season-pass numbers.

**Multiplayer backend is LIVE and the full matchmaking spine is built end-to-end (2026-06-07).**
On the box `5.78.110.182` (CPX31/`hil`): **Nakama** (`:7350` — device-auth identity, 66 leaderboards, `submit_score` RPC, forming-lobby match handler + matchmaker hook) and the **Godot match server** (UDP `8771` — room router hosting many concurrent matches). The client is fully wired: device auth → the 3 leaderboard surfaces on live data → **Find Match → matchmaker → forming lobby (X/8 + vote) → JOIN_ROOM a Godot room → networked match**. **Seven commits** `f178d01`→`e53d53c`, each phase verified headless or live against the box. Full per-phase build record under "Next step" below.

**Design context (drove the build):** session-4 specs `notes/matchmaking_orchestration.md` (orchestration spine — queue → forming lobby, unanimous-of-present vote 4–7, auto-at-8, floor 4, speed-beats-quality, crash=void), `design/CAMPAIGN.md`, `design/DESIGN_MODES.md`; session-3 spine `notes/resim_contract.md`, `leaderboard_schema.md`, `ghost_ladder.md`, `leaderboard_ui_spec.md`. Identity: device-auth now, Steam later (one identity, display name = Steam persona).

## Next step

### ✅ DONE 2026-06-08 (CC) — campaign tutorial follow-ups (from the 2026-06-08 copy pass)
`campaign_verify` is green across all 5 missions (the PASS-1 decouple in `08d2d42` fixed the false M2/M3/M4 failures). Status of the six items:
1. ✅ **Ghost outline clears on deviation** — `build_guide.gd refresh()` retires the whole outline when a tower lands off the suggested set (`_suggested` tracks the full set); on-suggestion builds clear just that cell. Verified `campaign_verify` PASS 3 (`GUIDE ✅`).
2. ✅ **M3 outline rides the load beat** — already worked: M3's single `on_mission_load` beat carries `ghost_cells`, `set_prompts` fires at load and persists into the build phase (`tutorial_director._begin` → `_show_beat`).
3. ✅ **Thresholds stay, naming only** — fields kept (`*_threshold` = the 1/2/3-star cutoffs); UI already says "1–3 stars". No rename (optional cosmetic only).
4. ✅ **M1 win flow** — leave-only → next map / Trials / Ranked, done in the victory-screen rebuild.
5. ✅ **`grid_size` default → 25×16** — `map_resource.gd:16` already defaults to `Vector2i(25, 16)`.
6. ⏳ **Anchor check** — beat anchors (`score`/`respawn`/`tower`/`board`) resolving in the new right-rail HUD is **playtest-only** (not auto-testable). Also playtest-only: M1's blocking opener pause→resume. (`tutorial_callout._anchor_panel` still maps `score`/`upgrade_panel` to the OLD top-bar/right-dock positions — re-check against the rail layout during the playtest and adjust the offsets if they read wrong.)

**Also closed this session:** tutorial callouts are now **dismissable-only** (no 6s auto-dismiss; `tutorial_callout.gd`) — the last open sub-item under polish #9.

### ▶▶ NEXT SESSION — the human through-line (Steam-gated)
The MP spine + ranked scoring are built and every link is verified individually; what's left needs the live loop and people:
1. **Human 2-client E2E** — two real clients press **Find Match** → matchmake on Nakama → forming lobby → vote → play a FULL networked match across networks. Each link is verified (headless/live, records below); this is the manual all-the-way run. **Blocked on distribution** (no Steam = no clean way to hand the build to friends; itch/zip is a rejected hack — USER call 2026-06-07). (Ranked floor is 4, so exercising the *vote* path with <4 testers means temporarily lowering `LOBBY_FLOOR` in `index.js` + `restart nakama`, or auto-launch at the count you have.) **This is also where the ranked loop gets its first real exercise** — LP/MMR settle, the avg-MMR anchor across differing-skill players, Surface 2 with real opponents.
2. **Smaller tails:** the two human items (`end`-action vocab nod, an interactive solo playtest). (Ranked match-end submit + the server-owned Trials seed are now wired — see below.)
**To redeploy after server-side changes:** Godot server → rebuild Linux binary + `bash deploy/deploy.sh root@5.78.110.182`; Nakama JS module → `scp index.js` + `docker compose restart nakama` (NOT `up -d`).

### ✅ COMPLETE 2026-06-07 — Server-owned Trials map seeds (resim_contract §10 seed tail)
The last anti-cheat/fairness gap in solo Trials: the per-window map seeds were **client-derived** (`pve_select` `hash(date)+salt+tier*1013`), so a client could pick an easy map and post under the real board id. Now **server-owned** (`leaderboard_schema.md` §3).
- **`index.js` `trials_seeds` RPC** — returns `{daily:[5], weekly:[5], monthly:[5]}`; seeds are **stored-random per window-cycle** (system-owned storage, create-once with a `version:"*"` race guard), so they're stable until the window resets and identical for every player that cycle. Cycle index is computed from the **server clock** (client can't choose it). The later re-sim worker recomputes the cycle from a record's submit time to reject a non-canonical seed.
- **Client:** `NakamaBackend.fetch_trials_seeds` → `LeaderboardService.trials_seeds` → `pve_select._generate_all_windows` (now async) uses the server seeds, **falling back to the local window-identity derivation offline** (deterministic, just not authoritative — the LocalBackend honesty pattern). The canonical seed flows automatically into `map.seed` → `coordinator.sim_seed` → `map_ref` → the record → re-sim (`_map_ref_for` already reads `map.seed`).
- **Networked PVP seed was already server-owned** — `match_room.gd:60` = `hash(uuidv4 match_id)`; re-sim reads the actual seed from the record (`resim.gd:38`). So only the Trials gap needed closing.
- **Verified:** `trials_seeds_test` (headless — server seeds used; offline fallback deterministic + non-colliding) ✅; `leaderboard_test`/`sim_harness`/`ranked_lp_test` green; **LIVE** — deployed + `restart nakama`, `trials_seeds_live` confirms 5 seeds/window, **stable across calls**, windows distinct.

### ✅ COMPLETE 2026-06-07 — Ranked LP / MMR engine + Surface 2 (the deferred ranked-scoring piece)
The last deferred piece of the ranked loop. A networked PVP match end now **settles LP/MMR, persists it, mirrors the new ladder value to `ranked_s1`, and shows Surface 2** (`notes/pvp_ladder.md` + `leaderboard_schema.md` §4 + `leaderboard_ui_spec.md` §2). USER decisions: **wire MMR through now** (real lobby-avg anchor, not stubbed) + **full vertical**.
- **`src/scripts/ranked_ladder.gd`** (NEW, pure static) — MMR-anchored net-positive: base curve (percentile-mapped sub-8 so 1st=+45/last=−45), MMR amplify/dampen factor, stickiness floors (off in Masters; structurally redundant given a multiplicative factor + signed base table — kept as a spec-faithful guard), promotion, demotion buffer (→75 LP of lower tier), Bronze floor, Masters no-demote-out + uncapped, Elo MMR update. Single sort key = ladder value (`tier_base+LP`); tier math reused from `LeaderboardService.ranked_tier`/`RANKED_BANDS`. **Every number is a playtest dial; shape is locked.**
- **`save_data.gd`** — `data.ranked {season, value, mmr}` seeded lazily (Bronze 0, neutral MMR); `ranked_value/ranked_mmr/ranked_season/record_ranked_result`.
- **MMR plumbing (Nakama side ONLY — the verified Godot match server is untouched):** client MMR → `lobby_client` **OP_HELLO** → the `index.js` lobby handler averages present members → **`avg_mmr` in the GO message** → `queue_controller` stashes it on `SceneManager.pending_ranked_avg_mmr` → read at match end. (The matchmaker ticket gains MMR later, when MMR-banded matchmaking is built.)
- **`match_end_panel._show_pvp_ranked`** (Surface 2) — placement · LP block (tier · `47→77` · +LP chip · progress bar · "to next") · promotion/demotion note · FINAL ORDER rows (your row highlit, OUT for eliminated) · View season ladder / Queue again. Gated by `map_loader` on networked PVP (`transport != null`); offline bot practice keeps the plain placement panel. `scene_manager.report_ranked_result` mirrors the value via the existing `submit_score` RPC path (op `set` + record blob for the re-sim worker). LP is **client-computed for the beta** (consistent trust model).
- **Verified:** `src/tools/ranked_lp_test.{gd,tscn}` (headless — engine exhaustive + Surface 2 builds & persists, all ✅); `ranked_shot.{gd,tscn}` screenshot reproduces the mockup; regression (`sim_harness`/`leaderboard_test`/`ghost_ladder_test`/`campaign_verify`) all green; **LIVE** — `index.js` deployed to the box + `restart nakama` (runtime reloaded clean), `forming_lobby_test` extended → **`GO avg_mmr == lobby average (250)`** ✅ plus all prior lobby checks.
- **Untested (Steam-gated):** the full 2-human ranked match E2E — engine/UI/submit/anchor verified in isolation; the all-the-way live run is the human session.

### ✅ COMPLETE 2026-06-07 — Nakama client + leaderboards + matchmaking spine (build record)
**✅ Nakama is DEPLOYED & VERIFIED (2026-06-07, CC over SSH).** Box `5.78.110.182` (CPX31 @ `hil`, Ubuntu 26.04): Nakama healthy, external `:7350` → HTTP 200, **66 boards** (5 campaign + `ranked_s1` + 60 Trials) confirmed in Postgres, `submit_score` RPC registered. Console/gRPC are loopback-bound (tunnel: `ssh -L 7351:localhost:7351 root@5.78.110.182` → `http://localhost:7351`; login in gitignored `deploy/nakama/console_login.local.txt`). Server key lives in the box `.env`; pull it into the gitignored `src/nakama_local.cfg` for the client (USER chose "read from local config"). **Two deploy fixes** (both in `deploy/nakama/README.md` STATUS): Docker wasn't actually installed (CC installed it); compose entrypoint had a YAML folded-scalar bug that dropped `--database.address` (fixed to literal block). **No "confirm it's up" step needed — it's up; build directly against `:7350`.**

Then build, in order (each testable against the live instance):
1. **nakama-godot client + device auth — ✅ DONE & VERIFIED 2026-06-07.** Vendored the `com.heroiclabs.nakama` addon (v3.4.0) into `src/addons/`; added two autoloads — the addon's `Nakama` factory + a **`NakamaService`** autoload (`src/scripts/nakama_service.gd`). NOT named `NakamaClient` (that's the addon's class_name — would shadow it). It reads host/port/scheme/server_key from gitignored `src/nakama_local.cfg` (template `nakama_local.cfg.example`; key pulled from box .env), does **device auth** (stable per-install UUID in SaveData), persists+restores the session token (refresh-aware), and exposes the realtime socket. **Verified headless against the live `:7350`** (`src/tools/nakama_auth_test.{gd,tscn}`, RESULT OK): device auth → real account created → `getAccount` matches → WS socket `Connected!`. Gotcha logged: vendoring a class_name addon needs a one-time `--headless --import` to register the types ([[reference_godot_vendored_addon_import]]).
2. **`NakamaBackend` — ✅ DONE & VERIFIED 2026-06-07.** `src/scripts/nakama_backend.gd` implements all 5 reads (trials/neighborhood/rank via tournament records + around-owner; campaign + ranked via leaderboard records; ranked banded into RANKED_BANDS with `you`/`to_next`/`total`) + a `submit(kind, board_id, score, record_b64)` via the `submit_score` RPC. Nakama returns score/rank as **strings** — int()'d everywhere. **`LeaderboardService` reads are now `await`-tolerant** (5 methods `await backend()…`; 6 call sites in browse/match_end/pve_select await — `await` on a sync value collapses, so LocalBackend + the existing 26-check `leaderboard_test` are unchanged). `NakamaService.connect_backend()` calls `set_backend(NakamaBackend.new(self))` on success; **`boot.gd` auto-connects** in the background (non-blocking; test scenes bypass it). **Submit path is real**: `scene_manager.report_match_result` posts Trials/campaign scores via `_post_online` (encodes the record synchronously, then fire-and-forget RPC; no-op offline via `has_method("submit")`). **Verified** (`src/tools/nakama_backend_test.{gd,tscn}`, 23 checks, RESULT OK): 3 bot accounts + me seeded on daily-trials/campaign_m01/ranked_s1 → service reads return real ranks, neighborhood, is_me, campaign my_score, ranked banding + `you` standing. Test records + bot accounts deleted afterward (boards back to empty).
3. **Matchmaking** (bigger; `matchmaking_orchestration.md`) — ticket + escalation → forming lobby (fill X/8, unanimous-or-auto-at-8, abstain=no, no timeout) → instant-join → hand clients the **Godot match server** address + `match_id`. Authority stays in Godot; Nakama only matchmakes + points. **Decomposed into 3a–3d:**
   - **3a. Concurrent rooms — ✅ DONE & VERIFIED 2026-06-07.** The dedicated server is now a **room router** (`net/match_server.gd` rewritten): one process hosts N matches keyed by `match_id`, each an isolated authority subtree built WITHOUT `change_scene` (`net/match_room.gd`) with a per-room transport adapter (`net/room_transport.gd`) that scopes broadcast to its peers. Clients JOIN_ROOM (`net_protocol.gd`); a room auto-starts at `expected` members and self-destroys on match end (`NetMatch.match_finished` signal replaced `reset_dedicated_lobby`, now removed). Client side untouched (a client is only ever in one match). **Verified** (`src/tools/rooms_test.{gd,tscn}`, 20 checks, RESULT OK): 2 rooms × 2 peers in one process — distinct coordinators, per-peer START_MATCH seats, build-input/clock routed within each room (no cross-talk, structurally guaranteed by RoomTransport's peer set), forfeit-teardown of one room while the other runs. **Note:** the legacy single-lobby client path (`lobby.gd` SET_NAME/PLAY/LOBBY_STATE) is now stale — it gets rewired onto JOIN_ROOM in 3d. Cross-network ENet (2 real clients) remains the human integration test.
   - **3b. Nakama matchmaker — ✅ DONE & VERIFIED 2026-06-07.** `src/scripts/matchmaker.gd` (`Matchmaker` Node): submits a Nakama matchmaker ticket over the realtime socket and runs an escalation schedule (widen query + lower count floor 8→6→4 over time, `RANKED_SCHEDULE`; timings/band-widths are dials), emits `matched({match_id, token, ticket, users})` on a pop. **Verified** (`src/tools/matchmaker_test.{gd,tscn}`, RESULT OK): 2 sessions (me + bot), each own socket + Matchmaker, submit compatible tickets → both receive `matched` with the 2 expected users + a join token. The `token` is the handoff to 3c (join the forming-lobby handler). Gotcha logged: GDScript lambdas capture locals by value ([[reference_gdscript_lambda_capture]]) — the first test run looked like "no match" but was a capture bug.
   - **3c. Forming lobby + vote — ✅ DONE & VERIFIED (live) 2026-06-07.** Authoritative **`lobby` match handler** in `deploy/nakama/data/modules/index.js` + a `matchmakerMatched` hook. **Accreting model** (chosen): each matchmaker pop routes into the OPEN lobby for its mode via `matchList` label query (`+label.mode:.. +label.open:1`), else creates one — so a lobby fills X/8 across pops. Auto-launch at 8; **unanimous-of-present** vote-launch at floor 4–7 (abstain=no, op codes VOTE/LOBBY_STATE/GO); on launch it broadcasts **GO `{match_id, host, port}`** (the Godot room to join) and terminates. Client = **`LobbyClient`** (`src/scripts/lobby_client.gd`): join via the matched `match_id`, receive state, `vote()`, receive GO. **Deployed** (module reload needs `docker compose restart nakama` — `up -d` won't, since `data/` is a volume mount; README §3 fixed). **Verified live** (`src/tools/forming_lobby_test.{gd,tscn}`, 11 checks, RESULT OK): 4 sessions in **2 pops** → second pop **accreted into the same lobby** (→4), unanimous vote → all four got an identical GO with `host=5.78.110.182 port=8771`. Gotcha logged: use `restart` not `up -d` to reload a volume-mounted JS module.
   - **3d. Handoff + E2E — ✅ DONE & VERIFIED (live) 2026-06-07.** **Godot match server (rooms build) DEPLOYED to the box** via `deploy/deploy.sh` (rebuilt Linux binary + systemd `wend-server.service`) — `active`, bound **UDP 8771**, logs `room router up`. **`QueueController`** (`src/scripts/queue_controller.gd`) is the client glue: Matchmaker → LobbyClient → on GO `{host,port,match_id,count}` → `SceneManager.net_join(host)` → `JOIN_ROOM` (tier derived from match_id) → on `START_MATCH` → `start_networked_pvp(seed,tier,count,seat,names)`. **`lobby.gd` rewired** off the legacy connect-by-IP model onto QueueController (Find Match → queue → lobby X/8 + Launch-vote); `DEFAULT_SERVER` removed (addr now comes from Nakama's GO → `MATCH_SERVER_HOST` in `index.js`). GO gained `count`. **Verified live** (`src/tools/room_e2e_client.{gd,tscn}`, 2 real ENet client processes → the deployed box): both `JOIN_ROOM`'d the same room → server formed it at 2 → both got `START_MATCH` with distinct seats (0/1), same seed; server log shows `created → 2 peers → started → ended (clean teardown)`. **Step 3 matchmaking spine is functionally complete end-to-end.**
   - **Remaining = human E2E:** two real clients pressing Find Match → matchmake on Nakama → forming lobby → vote → play a FULL networked match across networks (each link is verified headless/live; the all-the-way-through-with-humans run is the last manual check). Also: ranked LP/results settle on match end (Surface 2) is still the deferred ranked-scoring piece.

Gotchas to remember: no Docker/Node on the dev box (couldn't local-test the Nakama JS — verify on the box); the `submit_score` RPC trusts the client's re-sim'd score for the beta (server-side re-sim worker is deferred launch-hardening, blobs are already stored for it); Steam auth is a separate later task (GodotSteam SDK).

### ✅ §4.1 legality check (+ record serialization) — DONE 2026-06-08
The re-sim now **rejects tampered logs**, closing the second anti-cheat half (it already closed score injection; this closes *illegal* logs). All in `src/scripts/resim.gd` + the submit path, verified headless.
- **`_apply` validates every action at its tick** against the replayed authoritative state and returns a reason on failure (nothing is applied when illegal): `place` (bool of `bot_place_tower` = affordability + `_is_valid_placement`), `sell` (`_sell_tower_at_cell` false ⇒ no tower), `upgrade` (tower exists + `can_upgrade` not-maxed + `can_afford` — previously force-spent), unknown action type, bad seat. **Phase gate:** place/sell/upgrade rejected at any `phase == "run"` tick.
- **`run()` returns `legal: bool` + first-illegal `{tick, seat, action, reason}`**; the first illegal action stops the replay. **Submit path** (`scene_manager._authoritative_score`/`report_match_result`) now returns `{score, legal}` and **writes no score** when illegal.
- **Record serialization:** `Resim.encode_record`/`decode_record` (`var_to_bytes`/`bytes_to_var`; Vector2i cells; ~2.5 KB/record) — the wire/store format for the submit path.
- **Verified** (`src/tools/sim_harness.gd`, 5 checks ✅): honest round-trip (re-sim==live, dmg=54985) · serialize→deserialize identical · two tampered copies rejected (occupied cell ⇒ `illegal_place`; build action at a run tick ⇒ `phase_gate`) · inflated claim ignored · illegal log writes no score. **Note:** the check exposed that the harness had been applying *free* upgrades (direct `t.upgrade()` bypassing gold) — fixed to pay through the real economy, which dropped the honest baseline from an inflated 69962 to a legitimate 54985.

### ▶ Nakama meta backend — IN PROGRESS 2026-06-08 (stack authored; box provisioned @ hil; ✅ DEPLOYED & LIVE 2026-06-07 — client wiring is next)
Bringing the leaderboard service's backend online (and, after, matchmaking). **Decisions (2026-06-08):** self-host Nakama on a dedicated Hetzner box — **`5.78.110.182`, CPX31 (4 vCPU / 8 GB), `hil`/us-west** (moved off the old CPX11/2 GB at `ash`, which was too tight for Nakama+Postgres next to the match sim; Heroic Cloud rejected on cost ~3–5×). **Device-auth first, Steam later.** Scope = **leaderboards + matchmaking** (leaderboards first).
- **Architecture call (resolves a doc conflict):** match **authority stays in the headless Godot server**; **Nakama = meta/matchmaker only** (it hands clients a `match_id`/address). Picks `remote_beta_plan.md`'s "lower-code" path over `matchmaking_orchestration.md`'s Nakama-Go-handler suggestion — rewriting the verified GDScript coordinator in Go is wasteful. (orchestration doc explicitly allows this: "only where the code runs changes.")
- **CC AUTHORED — `deploy/nakama/`** (ready to deploy, not yet live): `docker-compose.yml` (Postgres 16 + Nakama 3.22.0, private Postgres, ports 7349/50/51), `data/modules/index.js` (JS runtime — idempotently creates **5 campaign leaderboards + ranked_s1 + 60 Trials tournaments** per `leaderboard_schema.md` with UTC reset crons, plus an authoritative **`submit_score` RPC** that writes server-side + stashes the match record blob for the later re-sim worker — the real wire format, validator deferred = "no disposable intermediates"), `.env.example`, `.gitignore`, `README.md` (provision + Docker + deploy + verify). **Couldn't local-test** (no Docker/Node on the dev box) — verified by hand against the 3.x runtime API; real verify is on deploy.
- **✅ USER provisioned the box (2026-06-08):** fresh CPX31 created at `hil` (`5.78.110.182`), firewall `firewall-1` set to 3 inbound rules (TCP 22, TCP 7350, UDP 8771 — all any-IP; console is SSH-tunnel-only, no public 7351). Did **not** rescale — CPX11 is deprecated Gen1 and `ash` had no CPX31 capacity; fresh create + redeploy was cleaner since nothing was wired yet. Old box deleted. **✅ DEPLOY DONE (CC, 2026-06-07, over SSH):** Docker installed (29.5.3), stack uploaded to `/opt/wend-nakama`, `.env` generated (random secrets, perms 600), `docker compose up -d` → healthy, 66 boards verified, `:7350` public-reachable. Fixed a compose folded-scalar bug that crash-looped Nakama. Then **CC builds** (against the live instance so it's testable): the nakama-godot client + device auth (`NakamaClient` autoload) → a **`NakamaBackend`** for `LeaderboardService` (reads via list/around-owner, writes via `submit_score`) → `set_backend()` it and the 3 leaderboard surfaces light up with real data → then **matchmaking** (ticket→forming-lobby→hand off to the Godot server, which then needs concurrent-match support).
- **CC deploy-time hardening (small, `notes/open_items.md` → Deploy/ops):** bind the Nakama console to loopback in `docker-compose.yml` (`127.0.0.1:7351:7351`) on the first deploy pass — tunnel-only even if the firewall is misconfigured. 7350 stays public.

### ✅ Leaderboard UI surfaces (3 of 4) + data-service seam — DONE 2026-06-08
Built `notes/leaderboard_ui_spec.md` Surfaces 1/3/4 against a backend-abstracted service — **the seam Nakama plugs into with zero UI rework.**
- **`src/scripts/leaderboard_service.gd`** (NEW): the single read API every surface uses. Owns store-independent logic (board-id naming `trials_<window>_<scale>_<group>`, scale/window names, **UTC reset countdowns** per schema §5.1, Ranked tier→band math) and delegates the row fetch to a swappable backend. Ships **`LocalBackend`** — honest: real local best scores (`SaveData`) + live countdowns, **empty competitor lists** (correct pre-server, not a placeholder). `set_backend(NakamaBackend.new())` later lights everything up. Static-only (no autoload).
- **Surface 3 — `leaderboard_browse.gd` (NEW) + scene + home button:** the hub. Category seg Trials/Ranked/Campaign; Trials = window tabs + countdown + group seg + scale pills + ranked rows w/ jump divider + your green row; Ranked = season seg + your-standing card + one continuous tiered-**band** ladder (Masters→Bronze, colored tags); Campaign = per-mission boards. Empty-states where there's no local data. Deep-linkable via `SceneManager.goto_leaderboards(ctx)`.
- **Surface 4 — `pve_select.gd`:** cards renamed Scale 1–5 → **Thread/Weave/Tangle/Snarl/Knot**; inline live-rank chip ("#14 ›") that taps through to that board. (Also satisfies part of the locked scale-name label-pass.)
- **Surface 1 — `match_end_panel.gd`:** Trials-only post-match placement block (board context · "You placed #N today" · neighborhood ±2 · **View full board**), below the tier strip; panel now sizes to content. Wired via `match_end.lb_ctx` from map_loader (PVE only; campaign unchanged).
- **Surface 2 (Ranked post-match LP/placement): ✅ DONE 2026-06-07** — built with the LP/MMR engine (see "Ranked LP" record above). Was deferred here pending the engine; now wired end-to-end + verified live.
- **Verified** (`src/tools/leaderboard_test.{gd,tscn}`, headless, **26 checks ✅**): service logic (naming/bands/countdowns) · LocalBackend honesty (empty vs own-entry) · browse renders all 3 categories with an injected sample backend (jump divider, category/selection switches) · entry points (select builds; PVE match-end gets lb_ctx + renders rows; campaign does not). Screenshots (`leaderboard_shot`) eyeballed vs mockups. ghost_ladder/campaign_verify/sim_harness still green.
- **`ui_style.gd`:** added public `flat_box()` for one-off row/tag surfaces.

### ✅ Ghost ladder (in-match target display) — DONE 2026-06-08
Built `notes/ghost_ladder.md` for Trials (PVE). The in-match SCORE pill's target line now climbs a merged ascending ladder instead of medals-only: **named tier → ghost score → your best → TOP**, never asserting a live rank in-match.
- **`src/scripts/ghost_ladder.gd`** (NEW, `GhostLadder` RefCounted): `setup(b,s,g, ghosts, own_best)` builds the rung list (named tiers below gold ++ snapshot ghost scores above gold); `target_for(score)` returns `{state,target,label,name}` driving the 4 states; `passed()`/`rung_count()` for the optional counter. **Snapshot source is a stub** — `fetch_snapshot()` returns `[]` (no Nakama yet), so offline play correctly falls through to YOUR BEST / TOP (population-independent by design); the GHOST state lights up unchanged when the backend lands.
- **`hud.gd`** — SCORE pill target line routes through `ghost_ladder` when set (medal icon only in named-tier states; badge text carries GHOST/YOUR BEST/TOP). Persistent **"standings as of match start"** caption (the honesty contract). Campaign (`ghost_ladder == null`) keeps the medal-only target.
- **`map_loader.gd`** — `_build_ghost_ladder(map)` builds it for **PVE only** (own-best from `SaveData.best_pve_score`), assigned to HUD before add_child. **Removed the gold-reached "go home?" win panel for Trials** (`_build_match_ui` now gates `WinPanel` to CAMPAIGN) — a Trials run goes until its rounds are spent.
- **Verified** (`src/tools/ghost_ladder_test.{gd,tscn}`, headless, 19 checks ✅): full climb through all 4 states (sub-gold ghost ignored) · dead-board + brand-new-player fallbacks · passed()/rung_count() · wiring (PVE HUD has a ladder + no win panel; campaign has neither change). campaign_verify + sim_harness still green.
- **Next when MP lands:** point `GhostLadder.fetch_snapshot()` at the cached per-`(map,window,group)` leaderboard read; surface the live-rank reveal on the result screen (the only live number).

### ▶ NEXT SESSION — pick up the remaining resim_contract §10 tail + the two human items
The anti-cheat spine is now complete for solo Trials/PVE (determinism → record → re-sim → legality → authoritative write, all verified). What's left:
- **Submit path plumbing** — `encode_record` exists but nothing calls it yet; the actual Trials submit (record → bytes → server/Nakama) is a networked-MP task, sequenced with the orchestration spine.
- **Ranked placement** still reads the live coordinator (authoritative offline; networked host-side re-sim is a later MP task).
- **Wire the real server seed** into `sim_seed` (today = map.seed).

**Two items that want Tobe (not blocking code):**
- **`end`-action nod** — the "locked" §9.2 vocab was extended with an `end` bow-out marker (flagged in `resim_contract.md`). Needs an OK or a different call.
- **Human playtest** — drive a real match interactively; the tick logic is exhaustively verified headless but the live UI/fast-forward path wasn't. De-risks the whole refactor; worth doing before building more on top.

---

- **CC — sim determinism conversion: DONE ✅ (2026-06-07).** The re-sim prerequisite (§5) is built and verified:
  - **§5.1 cross-platform float test:** floats bit-identical across Win/Mac/Linux-glibc → built on `float`, no fixed-point. Probe `src/tools/float_probe.gd` + CI guard `.github/workflows/float-probe.yml`; evidence `notes/float_probe_results.md`. (Caveat: if the prod re-sim server is musl/Alpine, add a musl CI leg — glibc-only doesn't clear musl's libm.)
  - **Fixed logical tick:** all sim subsystems now stepped by one fixed-timestep clock in `match_coordinator.gd` (`SIM_DT`, `sim_tick`, accumulator + `MAX_STEPS_PER_FRAME`). Towers/spawner/projectiles/mobs no longer self-`_process` — they expose `sim_step()` and are driven in a fixed order by `BoardState.sim_step` (spawn→towers→projectiles→mobs). Clients still sim locally (entity-step on every machine; clock host-only) so netcode is preserved.
  - **Seeded RNG, ordered draws:** the crit roll (was global `randf()`) now uses one per-match `coordinator.rng`, drawn in board→placement order. Only combat roll, so "all combat rolls seeded" is satisfied.
  - **Tick-based build timer:** `build_ticks_left` is authoritative; `build_time_left` (sec) is just the HUD mirror.
  - **Verified:** `src/tools/sim_harness.gd` (headless, tick-driven) runs a full 13-round match, **byte-identical across 2 runs**, 0 errors, build-timer auto-expiry exercised. Build-phase length proven not to leak into combat outcome.
- **CC — record capture + re-sim runner: DONE ✅ (2026-06-07).** resim_contract §2/§4/§7 built and round-trip verified:
  - **Record capture:** `coordinator.{record_enabled, input_log, map_ref, ruleset_version}` + `log_input(seat, action)` (stamps `sim_tick`) + `make_record()`. Capture sites: `build_controller._place_tower`/`_sell_tower_at_cell` (via `_log_action`), `tower.upgrade`, and `request_start_now`/`set_board_ready` (start/vote_start, §9.2). map_loader wires `sim_seed = map.seed`, `map_ref`, `record_enabled = true`.
  - **Re-sim runner:** `src/scripts/resim.gd` — rebuilds the map from `map_ref`, builds a headless match (recording off), replays the tick-tagged log through the same board entry points, derives per-board score.
  - **Round-trip verified** (`sim_harness.gd`, now a capture→re-sim test): a 13-round match with 42 logged actions across rounds (incl. a round-2 placement at tick 3559) re-sims to the **exact same score** (dmg=69962, kills=485). This is the keystone property — the leaderboard number is the re-sim's, and it matches honest play.
- **CC — wire outputs: DONE ✅ (2026-06-07).** `SceneManager.report_match_result` now records the **re-sim-derived** score, never the live client tally: it re-sims `active_coordinator`'s record (set in `main.gd`) and writes that to SaveData (campaign medal / PVE best). The live tally is advisory/UX only. A mid-match bow-out logs an `end` marker so re-sim scores the partial (new `end` action — extends §9.2, flagged in `resim_contract.md` for review). **Verified** (`sim_harness.gd`): handed an inflated claim of 99,999,999, SaveData stored the honest 69,962 — "you can't write score = 9,999,999." Locally the re-sim runs client-side as the server stand-in + a determinism self-check (`push_warning` if it ever disagrees).
  - **Remaining (resim_contract §10):** solo-log **legality check** (§4.1) **DONE 2026-06-08** (see "Next step") · **record serialization** **DONE** (`Resim.encode_record`/`decode_record`); the actual **submit** wiring (record → bytes → server) is still a networked-MP task · **Ranked placement** still reads the live coordinator (authoritative for offline; networked host-side re-sim is a later MP task). Also: wire the real server seed into `sim_seed` (today = map.seed); bot upgrade-pick uses unseeded `randi()` (`bot_controller.gd:154`) — fine, bot matches are never re-simmed.
  - **Needs a human:** real interactive playtest to confirm the live (frame-accumulator) path *feels* right — tick logic is exhaustively verified but the in-app UI/fast-forward flow wasn't driven headless.
- **CC label-pass (mechanical):** Scale 1–5 → Thread/Weave/Tangle/Snarl/Knot across `design/DESIGN_MODES.md` + `design/VISUAL_SYSTEM.md`; remove the Trials "go home?" prompt. (Deliberately not done at wrap to avoid full-rewrite drift.)
- **CC — campaign rebuild: DONE ✅ (2026-06-08).** Five-mission curriculum per `design/CAMPAIGN.md`, built + headless-verified:
  - **Maps** live in `src/campaign/` (the doc's `levels/campaign/` path was wrong). `mission_01–05.tres` rewritten to ramp from zero (M1 0CP full-ghost · M2 2CP first-segment ghost · M3 3CP hint ghost · M4 1CP + 4 zones · M5 3CP + 5 zones integration). `mission_06–10.tres` deleted; `CAMPAIGN_MISSION_COUNT`/`CAMPAIGN_MISSIONS`/`campaign_select.LESSONS` trimmed to 5. Tuning + B/S/G are **gentle uncalibrated stubs** (await the 25×14 retune + playtest).
  - **Tutorial-beat schema (reopen → resolved):** an array of `TutorialBeat` sub-resources on `MapResource.tutorial_beats` (mirrors `bonus_zones`/`obstacles`; generated maps leave it empty). `TutorialBeat` = trigger/text/anchor/ghost_cells/blocking.
  - **Runtime (local board, CAMPAIGN only):** `TutorialDirector` maps match signals → 7 triggers (one-shot); `TutorialCallout` = anchored toast + blocking modal (pauses tree for M1's opener); `BuildGuide` = dashed-tile + 40%-alpha-footprint ghost outline, clears a cell on build. No new art. Callouts are positioned (no pointer-arrows yet — deferred polish).
  - **Verified** (`src/tools/campaign_verify.gd`, headless): all 5 parse, every ghost-cell set is a legal maze (path stays open), director/callout/overlay build end-to-end. **Needs a human:** M1's blocking opener (pause→"Got it"→resume) isn't auto-testable headless — confirm in playtest.
- **Design — remaining big pieces:** juice/game-feel pass · Steam closed-beta mechanics · season-pass numbers · GTM. No design piece is currently blocking CC — the orchestration + campaign specs give CC a full plate.
- Still needs two humans: a real 2-client cross-network match (targets the end-state stack).

## Recently touched files
**Juice — in-match rail arrival + tower-deepen pop (2026-06-08, CC):**
- `src/scripts/rail.gd` — EDIT (three rail boxes cascade in on match start via scale+fade, staggered, armed-before-reveal; `_play_rail_arrival`)
- `src/scripts/tower.gd` — EDIT (emphasis-pop on the sprite at the instant a tower deepens on upgrade; guarded to in-tree sprite)
- `src/scripts/motion.gd` — EDIT (`pop` scales about the node's current scale; `slide_in` delay-vs-set_parallel bug fixed)

**Juice — victory choreography (2026-06-08, CC):**
- `src/scripts/match_end_panel.gd` — EDIT (campaign victory staged reveal via Motion: scrim dim → hero drop → star cascade+pop → DAMAGE tick+pop → button settle; armed-before-reveal; `_play_victory_choreo`/`_tick_score`/`_pop_after`)
- `src/tools/victory_shot.{gd,tscn}` — NEW (windowed capture harness: clears the M1 blocking tutorial, drives the victory, saves mid + settled frames)
- `design/JUICE.md` — EDIT (Implementation status: victory choreography ✅)

**Polish-list closeout + Wend rename (2026-06-08, CC):**
- `src/scripts/build_guide.gd` — EDIT (outline retires on deviation: `_suggested` set + `refresh()` clears whole outline on an off-suggestion tower; polish #9 follow-up #1)
- `src/scripts/tutorial_callout.gd` — EDIT (dismissable-only toasts: removed 6s auto-timer, "Tap to dismiss" hint; polish #9)
- `src/scripts/home_screen.gd` — EDIT (title "MAZE BATTLE TD" → "WEND"; tagline kept)
- `src/project.godot` — EDIT (`config/name` → "Wend")
- `src/scripts/{pve_select,match_end_panel,lobby,leaderboard_browse}.gd` — EDIT (em-dash sweep → middot "·" / rephrase; polish #3)
- `design/VISUAL_SYSTEM.md` — EDIT (PVE "em-dash if unplayed" → "No score yet")
- `src/tools/campaign_verify.gd` — EDIT (PASS 3: build-guide deviation unit test, `GUIDE ✅`)
- `src/tools/home_shot.{gd,tscn}` — NEW (home-screen capture harness: full shot + 8× corner crops; used for #1 white-corner diagnosis)
- `notes/polish_punchlist.md` — EDIT (#1/#2/#3 resolved with findings)
- `src/scripts/motion.gd` — NEW (the shared motion helper: timing tokens + 3 verbs + faithful arrive_ease + pop/fade/slide/cascade + reduced-motion; JUICE keystone)
- `src/tools/motion_test.{gd,tscn}` — NEW (headless pure-math verify: arrive curve / reduced-motion / stagger cap / tokens)
- `src/scripts/round_toast.gd` — EDIT (wave-clear toast now drops-from-top→hold→leave via Motion; a named JUICE beat)
- `src/scripts/leaderboard_panel.gd` — EDIT (in-match PVP drawer arrive-in / leave-out via Motion)
- `design/JUICE.md` — EDIT (added Implementation status + overshoot-dial discrepancy flag; the rest is the locked foundation from the design pass)
- `notes/mockups/juice_motion_reference.html` — NEW (the motion language: timing / easing verbs / stagger / pop / spatial grammar / break-grid)
- `notes/mockups/victory_screen_mock.html` — NEW (staged victory choreography; full-outline star tiles → polish #7)
- `notes/mockups/inmatch_hud_mock.html` — NEW (rail stagger-in + tower overlay over live board + color-deepen pop + build→run flip)
- `notes/mockups/meta_menu_mock.html` — NEW (home + Thread-Weave-Tangle select attitude pass + home→select screen transition)
- `notes/mockups/staged_climbs_mock.html` — NEW (Ranked Surface 2 LP fill / promotion set-piece + in-match ghost-ladder climb)
- `notes/mockups/round_end_overlay_mock.html` — NEW (shared round-end beat: Trials gold/score deltas / Ranked pairwise lives swing)
- `notes/tools/map_editor.html` — NEW (25×16 hand-authoring map editor: paint roles + zones, lossless `.tres` import, saves `mission_NN.tres`; resolves polish #9 editor sub-task)
- `src/campaign/mission_01–05.tres` — REWRITTEN this session (Tobe's editor layouts + zones + ghost outlines, with reviewed tutorial beats restored, correct names/descriptions). Names: First Contact · The Long Way · Switchbacks · Hot Spots · The Gauntlet
- `design/INMATCH_HUD.md` — NEW (in-match HUD layout: reserved rail + maximized 25×16 board + contextual tower overlay; signed off 2026-06-07)
- `notes/mockups/inmatch_assembly.html` — NEW (1080p reference mock of the locked layout)
- `notes/polish_punchlist.md` — NEW (9-item polish/bug list from the 2026-06-07 review; CC tasks)
- `src/scripts/match_coordinator.gd` — fixed-step sim clock + seeded rng + tick build timer + record capture
- `src/scripts/round_manager.gd` — `BoardState.sim_step` ordered stepping + projectiles array
- `src/scripts/{tower,spawner,projectile,mob}.gd` — `_process`→`sim_step` (externally driven); tower logs upgrades
- `src/scripts/build_controller.gd` — logs place/sell actions (`_log_action`)
- `src/scripts/map_loader.gd` — wires `sim_seed`/`map_ref`/`record_enabled` + `_map_ref_for`
- `src/scripts/resim.gd` — authoritative re-sim runner; honors `end` marker; **§4.1 legality (`_apply` validates + returns reason, phase gate, `legal`/`illegal` in `run()`) + `encode_record`/`decode_record`** (2026-06-08)
- `src/scripts/scene_manager.gd` — score write reads from re-sim; **`_authoritative_score` returns `{score, legal}` and the submit path writes no score on an illegal log** (2026-06-08)
- `src/scripts/main.gd` — sets `active_coordinator` for authoritative scoring
- `src/tools/sim_harness.gd` — determinism + round-trip + **legality/serialize/reject** regression harness; **upgrades now pay through the real economy** (2026-06-08)
- `src/tools/float_probe.gd` — NEW (§5.1 cross-platform float probe)
- `.github/workflows/float-probe.yml` — NEW (matrix CI determinism guard)
- `notes/float_probe_results.md` — NEW (float test result: floats safe ✅)
- `notes/matchmaking_orchestration.md` — NEW (orchestration spine)
- `design/CAMPAIGN.md` — NEW (five-mission rework + tutorial beats + ghost outline)
- `design/DESIGN_MODES.md` — EDIT (Trials reconciled, campaign cut to five, grid drift flagged)
- `STATE.md`, `notes/open_items.md` — updated this session

**Backend box move (2026-06-08):**
- `deploy/nakama/README.md` — EDIT (§0 resize→fresh-create at `hil`; firewall = 3 public rules + SSH-tunnel for console + loopback-bind TODO; new IP `5.78.110.182` throughout)
- `notes/open_items.md` — EDIT (new Deploy/ops section: box move recorded + CC loopback-bind task; beta_brief stale-framing flagged in Drift/audit)
- `notes/beta_design_brief.md` — EDIT (IP swap only — `178.156.171.215`→`5.78.110.182`; stale itch.io/Steam framing left for next audit)
- `src/scripts/lobby.gd` — EDIT (CC: `DEFAULT_SERVER` IP swap, one-line constant)

**Campaign rebuild (2026-06-08):**
- `src/resources/tutorial_beat.gd` — NEW (beat sub-resource: trigger/text/anchor/ghost_cells/blocking)
- `src/resources/map_resource.gd` — EDIT (`tutorial_beats: Array` field)
- `src/scripts/tutorial_director.gd` — NEW (signals → 7 triggers, one-shot)
- `src/scripts/tutorial_callout.gd` — NEW (anchored toast + blocking modal)
- `src/scripts/build_guide.gd` — NEW (dashed-tile + footprint ghost outline)
- `src/scripts/map_loader.gd` — EDIT (instantiates director/callout/guide for local CAMPAIGN board)
- `src/campaign/mission_01–05.tres` — REWRITTEN (five-mission ramp + beats); `mission_06–10.tres` DELETED
- `src/scripts/scene_manager.gd` — EDIT (CAMPAIGN_MISSIONS/COUNT → 5)
- `src/scripts/campaign_select.gd` — EDIT (LESSONS → 5, grid is now one row)
- `src/tools/campaign_verify.{gd,tscn}` — NEW (headless resource+maze+director verifier)

## Open questions / blocked on
**Board count LOCKED 25×16 (2026-06-07)** — replaces 25×14; universal, derived off 1080p, scale+center elsewhere. **Campaign rebuild + map editor now unblocked** (board size was the dependency); next-session candidate is the hand-authoring grid editor (color cells: board / obstacle / tower-ghost / checkpoint, + resizable zone circle) → see `notes/polish_punchlist.md` item 9. Minor open from the HUD pass: Speed button during build = present-but-disabled vs empty-until-run. Stat-row [+] / star icons are still placeholder (real-asset gap). Tower-info overlay-vs-in-rail is a live implementation tradeoff (it fits in the rail's lower gap) — locked as overlay, but in-rail is a fair alternative at build time.
Full per-item status in `notes/open_items.md`. Active design: juice (parked, Tobe reframing) · season-pass numbers · Steam closed-beta mechanics · GTM.

**Polish #9 update (2026-06-08):** the hand-authoring grid editor is BUILT (`notes/tools/map_editor.html`) and the 5 campaign maps are re-authored with reviewed tutorial copy. Remaining #9 sub-items are CC tasks (see the six follow-ups under Next step). **Medals clarified:** there is no bronze/silver/gold naming — it's **1/2/3 stars**; the `*_threshold` fields stay (they're the star cutoffs), only the labels change. Tutorial copy already avoids medal words.

**Juice (2026-06-08):** Foundation LOCKED → `design/JUICE.md`; every distinct surface mocked (`notes/mockups/`). Open **dials** (numbers, not shape — tune in playtest): hero tilt −3.5°, set-piece stagger 130ms. The **optional impact-event audio sting** (victory / rank-up / high-score only) is flagged and **undecided** — Tobe's call. Ready for CC to implement against the doc + mocks.

**Steam (ops):** identity verification pending (2–7 biz days, started 2026-06-07) — blocks App ID + Playtest creation. Confirm entity type chosen at registration. Decide confidential-keys vs. public-Coming-Soon for the Playtest.
